function join(arr, concatStr) {
  for (var i = 0 ; i <= arr.length ; i++ )
    while ( i > 0 ) {
    arr.splice( arr[i], 0 , concatStr );
    return result
    }
}

var result = console.log(arr.concat(concatStr));
return result

/*function repeat(str, times) {
  
}*/

console.log(join(['a','b','c'], '!'));

/*console.log(repeat('a', 5));
/*用i+1的方式放
用double 的寫法嗎?
題目要求用return*/